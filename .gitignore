node_modules/
.env
.env.local
*.tmp
.vercel
.DS_Store

# Local/test state only — the GitHub Actions watch commits ./state.json on purpose,
# so state.json is intentionally NOT ignored.
state.test.json
state.filestore-test.json
